server:
  port: 8888
  
management:
  endpoints:
    web:
      exposure:
        include: "refresh"    
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///D:\workspace\bld\bld-config-repo



